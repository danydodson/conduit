module.exports = [
  'Architecture',
  'Carpentry',
  'Ceramics',
  'Code',
  'Collage',
  'Conceptual',
  'Decorative',
  'Drawing',
  'Electronic',
  'Film',
  'Food',
  'Glass',
  'Graphics',
  'Installation',
  'Light',
  'Literature',
  'Multimedia',
  'Natural',
  'Painting',
  'Performance',
  'Photography',
  'Printmaking',
  'Sculpture',
  'Sound',
  'Technical',
]

  // const BASE = `${process.env.REACT_APP_CL_BASE}`
  // const name = `${props.post.uploads[0].fileName}`
  // const type = `${props.post.uploads[0].response.body.type}`
  // const vs = `v${props.post.uploads[0].response.body.version}`
  // const format = `${props.post.uploads[0].response.body.format}`
  // const rs = `${props.post.uploads[0].response.body.resource_type}`
  // const med = `${props.post.medium}`
  // const opts = 'c_fit,q_80,'

  // const size01 = `${BASE}/${rs}/${type}/${opts}w_100/${vs}/${med}/${name}.${format}`
  // const size02 = `${BASE}/${rs}/${type}/${opts}w_200/${vs}/${med}/${name}.${format}`
  // const size03 = `${BASE}/${rs}/${type}/${opts}w_300/${vs}/${med}/${name}.${format}`
  // const size04 = `${BASE}/${rs}/${type}/${opts}w_400/${vs}/${med}/${name}.${format}`
  // const size05 = `${BASE}/${rs}/${type}/${opts}w_500/${vs}/${med}/${name}.${format}`
  // const size06 = `${BASE}/${rs}/${type}/${opts}w_600/${vs}/${med}/${name}.${format}`
  // const size07 = `${BASE}/${rs}/${type}/${opts}w_700/${vs}/${med}/${name}.${format}`
  // const size08 = `${BASE}/${rs}/${type}/${opts}w_800/${vs}/${med}/${name}.${format}`
  // const size09 = `${BASE}/${rs}/${type}/${opts}w_900/${vs}/${med}/${name}.${format}`
  // const size10 = `${BASE}/${rs}/${type}/${opts}w_1000/${vs}/${med}/${name}.${format}`
  // const size11 = `${BASE}/${rs}/${type}/${opts}w_1100/${vs}/${med}/${name}.${format}`
  // const size12 = `${BASE}/${rs}/${type}/${opts}w_1200/${vs}/${med}/${name}.${format}`
  // const size13 = `${BASE}/${rs}/${type}/${opts}w_1296/${vs}/${med}/${name}.${format}`
  // const size14 = `${BASE}/${rs}/${type}/${opts}w_1400/${vs}/${med}/${name}.${format}`
  // const size15 = `${BASE}/${rs}/${type}/${opts}w_1600/${vs}/${med}/${name}.${format}`
  // const size16 = `${BASE}/${rs}/${type}/${opts}w_1800/${vs}/${med}/${name}.${format}`
  // const size17 = `${BASE}/${rs}/${type}/${opts}w_2000/${vs}/${med}/${name}.${format}`
  // const size18 = `${BASE}/${rs}/${type}/${opts}w_2200/${vs}/${med}/${name}.${format}`
  // const size19 = `${BASE}/${rs}/${type}/${opts}w_2400/${vs}/${med}/${name}.${format}`
  // const size20 = `${BASE}/${rs}/${type}/${opts}w_2592/${vs}/${med}/${name}.${format}`

  // const srcset = `${size01} 100w, ${size02} 200w, ${size03} 300w, ${size04} 400w, ${size05} 500w, ${size06} 600w, ${size07} 700w, ${size08} 800w, ${size09} 900w, ${size10} 1000w, ${size11} 1100w, ${size12} 1200w, ${size13} 1296w, ${size14} 1400w, ${size15} 1600w, ${size16} 1800w, ${size17} 2000w, ${size18} 2200w, ${size19} 2400w, ${size20} 2592w`
